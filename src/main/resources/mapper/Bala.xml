<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com1">
    <!--
    <insert id="insertBala" parameterType="Bala" >
   INSERT INTO BALA VALUES (#{id},#{userName},#{age},#{mobilePhone})
    </insert>

    <select id="queryById" parameterType="int" resultType="Person">
        SELECT * FROM PERSON WHERE ID=#{id}
    </select>
    <select id="query" resultType="Person">
        select * from PERSON
    </select>
    <update id="updatePerson">
        UPDATE PERSON SET USERNAME=#{userName},AGE=#{age},MOBILEPHONE=#{mobilePhone} WHERE ID=#{id}
    </update>
    <delete id="deletePerson" parameterType="int">
        delete  from PERSON where ID=#{id}
    </delete>
    -->
    <select id="queryBala" resultType="Bala">
        select * from BALA;
    </select>
</mapper>